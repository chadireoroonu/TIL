import sys
sys.stdin = open('input.txt')

def backtraking(arr, k, n, result):
    c = [0] * 2
    if k == n:
        solution


T = int(input())
for tc in range(1, T+1):
    n = int(input())

    maxcandidates = 12
    nmax = 12

    data = [list(map(int, input().split())) for q in range(n)]
    arr = [[0] * n for q in range(n)]

    backtraking(arr, )