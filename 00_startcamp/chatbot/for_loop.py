numbers = [1, 2, 3, 4]
# numbers가 가진 모든 요소를 순회할 때 까지
# num 이라는 변수는 정해진 이름? no 변수 이름은 마음대로 설정
for num in numbers:
    # numbers가 가진 각 요소를 출력
    print(num)
print('끝났니?')

numbers = {'a': 1, 'b': 2}

for num in numbers:
    print(numbers[num])